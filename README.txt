** This is a Explanotory
  ** To create blocks, write var /** block name **/ = new block(
   block.color = array,
    block.minelength = int, 
    block.Ai.location.x = string, 
    block.Ai.location.z = array[high,low],
    block.fall = boolan,
    block.name = string);
  
